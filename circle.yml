version: 2

jobs:
  build:
    working_directory: ~/
    
    docker:
      - image: docker

    steps:
      - checkout

      - setup_remote_docker

      - run: |
          docker build -t quartic/uber-builder:${CIRCLE_BUILD_NUM} .
          docker login
          docker push quartic/uber-builder:${CIRCLE_BUILD_NUM}
